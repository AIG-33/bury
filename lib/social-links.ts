/**
 * Public social profiles of the project itself (not player socials — those
 * live in `profiles.social_*`). Telegram lives in `lib/telegram/bot-link.ts`
 * because it depends on env config.
 */
export const INSTAGRAM_URL = "https://www.instagram.com/playtennis.by";
